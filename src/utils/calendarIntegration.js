export const syncWithCalendar = (eventDate, eventName) => {
    // Dummy function to simulate calendar syncing
    console.log(`Event "${eventName}" synced to calendar for date ${eventDate}`);
    alert(`Event "${eventName}" synced to calendar for date ${eventDate}`);
  };
  